<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ankit.basechurch">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.DetailMember"></activity>
        <activity android:name=".activity.AddMembersData" />
        <activity android:name=".activity.TitheEditActivity" />
        <activity android:name=".EditAct" />
        <activity android:name=".activity.EditActivity" />
        <activity android:name=".Admin" />
        <activity android:name=".activity.AdminPage" />
        <activity android:name=".AdminActivity" />
        <activity
            android:name=".activity.Login"
            android:noHistory="true" />
        <activity android:name=".activity.HomeActivity" />
        <activity
            android:name=".activity.MainActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".activity.SplashActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>